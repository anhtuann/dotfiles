browser "qutebrowser %u"
auto-reload yes
macro y set browser "youtube-viewer -C --order=viewCount %u" ; open-in-browser ; set browser "elinks %u"

unbind-key h
unbind-key j
unbind-key k
unbind-key l

bind-key h quit
bind-key j down
bind-key k up
bind-key l open
